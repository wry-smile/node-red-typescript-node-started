import type { EditorRED } from "node-red";
import type { {{ properCase name }}ClientNodeProperties } from "./types";

declare const RED: EditorRED;

RED.nodes.registerType<{{properCase name}}ClientNodeProperties>("{{ kebabCase name }}", {
  category: "function",
  color: "#a6bbcf",
  defaults: {
    name: { value: "" },
  },
  inputs: 1,
  outputs: 1,
  icon: "file.png",
  paletteLabel: "{{ kebabCase name }}",
  label() {
    return this.name || "{{ kebabCase name }}";
  },
});
